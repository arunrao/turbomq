# Example environment variables for the browser example
# Copy this file to .env before running the example

# Database configuration (using SQLite by default)
DATABASE_URL="file:./prisma/dev.db"

# Socket.IO configuration
SOCKET_IO_PORT=3001
